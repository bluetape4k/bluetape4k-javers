# JaVers 0.2 manual diagram 검토

## 범위와 authority

- Release behavior authority: `0.2.1`, commit `bffe19439ca891fa5301a76421bdef7ba75252a0`.
- Prose authority: `docs/manual/{en,ko}` 아래 English/Korean Task 2-4 manual page.
- Source anchor: `CdoSnapshotRepository.kt`, `AbstractCdoSnapshotRepository.kt`, `ExposedCdoSnapshotRepository.kt`, `AggregateRepository.kt`, released `examples/javers-exposed-ddd` command/projection code.
- Visual authority: `/Users/debop/work/bluetape4k/bluetape4k-projects/docs/manual/assets/annotations/annotation-decision-map.png`.
- Sequence reference: `/Users/debop/work/bluetape4k/bluetape4k-wiki/docs/diagrams/best-practices/assets/sequence-workflow-sample.png` 및 `/Users/debop/work/bluetape4k/bluetape4k-javers/docs/images/readme-diagrams/javers-ddd-save-flow-01.png`.
- Loaded rule: `bluetape-diagram/SKILL.md`, `references/common.md`, `references/architecture.md`, `references/class.md`, `references/sequence.md`.
- Exposure: Task 5는 의도적으로 manual-page embed를 추가하지 않는다. Task 6이 canonical page integration을 소유한다.

다섯 SVG 모두 `1600x1040` viewBox를 사용한다. CairoSVG `-s 2`는 authoritative `3200x2080` PNG를 만들었다. Font는 `Architects Daughter`와 `Comic Mono`이며, palette는 restrained cyan, teal, purple, amber, rose, olive role을 가진 dark navy manual family다.

## Asset별 falsifiable ledger

### `overview/repository-learning-map`

- Reader question: 독자는 released repository responsibility를 어떤 순서로 학습해야 하는가?
- Model: audit model -> repository SPI -> Exposed/Redis/Kafka 중 하나 -> DDD path -> released example -> operations.
- XML/render: `xmllint --noout` succeeded; CairoSVG produced `3200x2080` RGB PNG.
- Common audits: `markers=12`, `connectors=9`, `cards=8`, `intrusions=0`, `crossings=0`, `geometry_failures=0`; endpoint audit PASS; mixed-corner `paths=9`, `q_bends=13`, `failures=0`.
- Original size eye review: card bounds 여덟 개가 모든 text를 포함한다. 14x14 head는 shaft color와 일치하고 direction을 읽을 수 있다. Exposed/Redis/Kafka는 별도 port와 contrasting corridor를 통해 SPI를 빠져나간다. 긴 DDD-to-example connector는 모든 card 밖에 머문다. Clipping이나 excess bottom whitespace는 없다.

### `architecture/audit-snapshot-model`

- Reader question: object, commit, metadata, snapshot, change, shadow, repository contract는 어떻게 관련되는가?
- Model: JaVers commit 하나는 metadata record 하나와 snapshot 하나 이상을 만든다. Change와 shadow는 snapshot에서 파생된다. Repository SPI는 encode, query, head state restore를 수행한다.
- XML/render: `xmllint --noout` succeeded; CairoSVG produced `3200x2080` RGB PNG.
- Common/class-style audits: `markers=12`, `connectors=8`, `cards=7`, `intrusions=0`, `crossings=0`, `geometry_failures=0`; endpoint audit PASS; mixed-corner `paths=8`, `q_bends=8`, `failures=0`.
- Original size eye review: solid creation/query와 dashed metadata association은 구분 가능하고 legend에서 설명된다. 모든 label은 border와 head를 침범하지 않는다. Body glyph는 replacement square 없이 render된다. Card를 cross하거나 enter하는 route는 없다.

### `persistence/persistence-decision-map`

- Reader question: 어떤 0.2.1 persistence adapter가 recovery responsibility에 맞는가?
- Model: durable SQL history는 Exposed를 선택한다. Accepted Redis snapshot store는 Redis를 선택한다. Publication-only는 Kafka를 선택한다. 모든 path에는 여전히 retry, stable ID, reconciliation, observability가 필요하다.
- XML/render: `xmllint --noout` succeeded; CairoSVG produced `3200x2080` RGB PNG.
- Common/architecture audits: `markers=12`, `connectors=9`, `cards=8`, `intrusions=0`, `crossings=0`, `geometry_failures=0`; endpoint audit PASS; mixed-corner `paths=9`, `q_bends=8`, `failures=0`.
- Original size eye review: initial title overflow와 corridor-label/card overlap은 긴 title만 22px로 줄이고 102px pill을 open corridor로 옮겨 수리했다. Final text는 모든 card 안에 머문다. 세 semantic color는 분리되어 있고 head 아홉 개가 crossing 없이 보인다.

### `persistence/exposed-snapshot-flow`

- Reader question: released Exposed repository의 transaction boundary는 어디인가?
- Model: `persist`는 commit을 lock한다. `saveSnapshot`은 operation마다 commit metadata와 snapshot 하나를 encode/write한다. 이어서 `updateCommitId`가 별도 transaction에서 `CommitTable`을 update한다. Load는 head와 snapshot을 독립적으로 recover한다.
- XML/render: `xmllint --noout` succeeded; CairoSVG produced `3200x2080` RGB PNG.
- Common/architecture audits: `markers=12`, `connectors=8`, `cards=7`, `intrusions=0`, `crossings=0`, `geometry_failures=0`; endpoint audit PASS; mixed-corner `paths=8`, `q_bends=8`, `failures=0`.
- Original size eye review: initial `updateCommitId`/`CommitTable` arrow direction은 final render 전에 고쳤다. Per-snapshot route와 after-snapshots route는 분리되어 있다. Non-atomic warning은 가려지지 않는다. 모든 head, port, label, card bounds가 보인다.

### `examples/ddd-cqrs-sequence`

- Reader question: released order example은 어떤 순서로 source state를 persist하고, audit하고, event를 publish하고, Redis를 update하는가?
- Model: `handle` -> `persist` -> saved aggregate -> `javers.commit` -> `publishAll` -> return, 그 뒤 asynchronous Kafka poll 및 Redis upsert.
- XML/render: `xmllint --noout` succeeded; CairoSVG produced `3200x2080` RGB PNG.
- Common audits: `markers=12`, `connectors=8`, `cards=0`, `intrusions=0`, `crossings=0`, `geometry_failures=0`; endpoint audit PASS; mixed-corner `paths=8`, `q_bends=0`, `failures=0`.
- Generic `cards=0`에 대한 targeted fallback: participant headers `7`, participant titles `7`, lifelines `7`, activations `4`, messages `8`, visible contiguous numbered labels `8`, transparent chronological frames `1`, explicit 16x16 marker를 사용하는 message connectors `8`; custom validator `failures=0`.
- Sequence-specific audit: best-practices outer frame, title/subtitle class, participant role, visible pill/number, lifeline, activation, transparent frame, established filled-triangle marker 기준으로 `PASS sequence_files=1`.
- Original size eye review: label 1-8은 각각 자신의 continuous line 위에 있다. Branch color와 head는 일치한다. Activation은 head를 숨기지 않는다. Async frame은 transparent다. 첫 review에서 rose failure note가 message 8과 overlap되어 frame을 `y=920`으로 늘리고 note를 baseline `y=895`로 옮겼다. Final clearance는 green pill bottom(`y=860`)보다 35px 아래, message line(`y=866`)보다 29px 아래다. Label-line overlap은 남아 있지 않다.

## Command와 최종 결과

```bash
for asset in repository-learning-map audit-snapshot-model persistence-decision-map exposed-snapshot-flow ddd-cqrs-sequence; do
  ruby scripts/manual/render_javers_diagrams.rb "$asset"
done
ruby scripts/manual/validate_diagrams.rb
find docs/manual/assets -name '*.svg' -print0 | xargs -0 python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-connector-audit.py
find docs/manual/assets -name '*.svg' -print0 | xargs -0 python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-geometry-audit.py --fail-diagonal
find docs/manual/assets -name '*.svg' -print0 | xargs -0 python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-endpoint-audit.py
find docs/manual/assets -name '*.svg' -print0 | xargs -0 python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-mixed-corner-audit.py
python3 ~/.codex/skills/bluetape-diagram/scripts/diagram-sequence-style-audit.py docs/manual/assets/examples/ddd-cqrs-sequence.svg
git diff --check -- scripts/manual docs/manual/assets docs/superpowers/notes/2026-07-16-javers-0.2-diagram-review.md
```

최종 기대 total: assets `5`, SVG `5`, PNG `5`, custom validation failures `0`, connector intrusions `0`, crossings `0`, geometry failures `0`, endpoint failures `0`, mixed-corner failures `0`, sequence-style failures `0`, unresolved visual defects `0`.
